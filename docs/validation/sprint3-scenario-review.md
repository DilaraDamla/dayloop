# Sprint 3 — Offline Scenario Validation Review

Generated by `tests/generateScenarioReview.js` — regenerate any time with:
```
node tests/generateScenarioReview.js
```

This artifact answers one question: **does DayLoop produce an acceptable day
across very different real-world environments?** It is a human-review
document, not a test report — machine-verifiable correctness is covered
separately by `tests/scenario-validation.test.js`. Read the two together:

1. **Machine-verifiable correctness** (below, and in the test suite): stop
   counts, tier usage, distances, hard-constraint enforcement, presence/
   absence of trade-off language, determinism. These are asserted and will
   fail CI-style if violated.
2. **Human-reviewed itinerary quality** (the "Human-review notes" under each
   scenario): whether a day actually *reads* well — narrative coherence,
   whether a village's one real landmark feels prioritized rather than
   arbitrary, whether explanation text feels natural rather than robotic.
   No automated check claims to prove these; they need an actual person to
   read the itinerary.

Environments and scenarios are defined in `tests/fixtures/environments.js`
and `tests/fixtures/scenarios.js` — both deterministic, offline, no network
calls. A documented limitation: this app has no real price/cost data source
anywhere in its pipeline, so "budget compatibility" is evaluated the same way
scorePOI evaluates it — by category (park/viewpoint/bakery/etc. lean free) —
never as an invented price figure.

**Summary: 160/160 machine-verifiable checks passed across 22 scenario runs.**

---

### Dense capital city — Calm day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: high (48 relevant POIs, 11 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_capital_0** (bakery) — tier: `core`, 45min, 0.15km from previous
  2. **gallery_capital_0** (gallery) — tier: `core`, 90min, 0.25km from previous
  3. **restaurant_capital_0** (restaurant) — tier: `core`, 90min, 0.29km from previous
  4. **cafe_capital_0** (cafe) — tier: `core`, 105min, 0.08km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Gallery is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Cafe is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan spans a fuller day with varied stops nearby.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=71
  - [PASS] hard constraint (excluded category) never appears — bakery,gallery,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] no duplicate categories in a dense area — duplicateCategoryCount=0
  - [PASS] compact route (avg leg < 0.8km) — avgLegKm=0.27
  - [PASS] sufficient stop variety (no category repeats) — distinctCategories=4/4

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Suburban area — Calm day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: medium (19 relevant POIs, 8 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_suburb_0** (bakery) — tier: `core`, 45min, 0.40km from previous
  2. **ice_cream_suburb_0** (ice_cream) — tier: `core`, 90min, 0.28km from previous
  3. **restaurant_suburb_0** (restaurant) — tier: `core`, 90min, 0.48km from previous
  4. **cafe_suburb_0** (cafe) — tier: `core`, 105min, 0.28km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Ice cream is close to your previous stop, keeping the route compact.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Cafe is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan is paced to match what you asked for.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=23
  - [PASS] hard constraint (excluded category) never appears — bakery,ice_cream,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] not forced into an urban (high-density) shape given medium supply — densityTier=medium
  - [PASS] coherent transitions (no backtracking) — backtrackCount=0
  - [PASS] mix of local and slightly-farther options — distances=0.40,0.28,0.48,0.28

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Calm day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (7 relevant POIs, 5 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_bakery** (bakery) — tier: `core`, 63min, 0.35km from previous
  2. **town_park** (park) — tier: `core`, 126min, 0.84km from previous
  3. **town_restaurant_far** (restaurant) — tier: `core`, 126min, 1.76km from previous
  4. **town_cafe_edge** (cafe) — tier: `core`, 147min, 3.25km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Park fits your calm, unhurried pace for today.
  3. This Restaurant fits your calm, unhurried pace for today.
  4. This Cafe fits your calm, unhurried pace for today.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop is a little farther away than usual.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — bakery,park,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop is a little farther away than usual.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Calm day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (3 relevant POIs, 3 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (2)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.62km from previous

**Per-stop explanations**
  1. This Cafe is close to your previous stop, keeping the route compact.
  2. This Restaurant fits your calm, unhurried pace for today.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=2, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=2
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=2
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Dense capital city — Budget day

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: outdoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: high (48 relevant POIs, 11 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_capital_0** (bakery) — tier: `core`, 45min, 0.15km from previous
  2. **artwork_capital_0** (artwork) — tier: `core`, 90min, 0.08km from previous
  3. **restaurant_capital_0** (restaurant) — tier: `core`, 90min, 0.27km from previous
  4. **bar_capital_0** (bar) — tier: `core`, 105min, 0.08km from previous

**Per-stop explanations**
  1. This Bakery is a lower-cost pick that fits your budget preference.
  2. This Public art is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Bar is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan spans a fuller day with varied stops nearby.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=71
  - [PASS] hard constraint (excluded category) never appears — bakery,artwork,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] no duplicate categories in a dense area — duplicateCategoryCount=0
  - [PASS] compact route (avg leg < 0.8km) — avgLegKm=0.20
  - [PASS] sufficient stop variety (no category repeats) — distinctCategories=4/4

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Suburban area — Budget day

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: outdoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: medium (19 relevant POIs, 8 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_suburb_0** (bakery) — tier: `core`, 45min, 0.40km from previous
  2. **ice_cream_suburb_0** (ice_cream) — tier: `core`, 90min, 0.28km from previous
  3. **restaurant_suburb_0** (restaurant) — tier: `core`, 90min, 0.48km from previous
  4. **bar_suburb_0** (bar) — tier: `core`, 105min, 0.28km from previous

**Per-stop explanations**
  1. This Bakery is a lower-cost pick that fits your budget preference.
  2. This Ice cream is a lower-cost pick that fits your budget preference.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Bar is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan is paced to match what you asked for.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=23
  - [PASS] hard constraint (excluded category) never appears — bakery,ice_cream,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] not forced into an urban (high-density) shape given medium supply — densityTier=medium
  - [PASS] coherent transitions (no backtracking) — backtrackCount=0
  - [PASS] mix of local and slightly-farther options — distances=0.40,0.28,0.48,0.28

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Budget day

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: outdoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (8 relevant POIs, 6 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_bakery** (bakery) — tier: `core`, 63min, 0.35km from previous
  2. **town_park** (park) — tier: `core`, 126min, 0.84km from previous
  3. **town_cafe_edge** (cafe) — tier: `core`, 126min, 2.24km from previous
  4. **town_bar** (bar) — tier: `core`, 147min, 1.42km from previous

**Per-stop explanations**
  1. This Bakery is a lower-cost pick that fits your budget preference.
  2. This Park is a lower-cost pick that fits your budget preference.
  3. This Cafe is a solid match for a budget-friendly day.
  4. This Bar is a solid match for a budget-friendly day.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop is a little farther away than usual.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — bakery,park,cafe,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop is a little farther away than usual.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Budget day

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `clear` (indoorBias=false)
- Duration requested: 7h (10:00–17:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: outdoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (4 relevant POIs, 4 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (3)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **The Old Mill Viewpoint — village landmark** (viewpoint) — tier: `core`, 126min, 0.80km from previous
  3. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.64km from previous

**Per-stop explanations**
  1. This Cafe is close to your previous stop, keeping the route compact.
  2. This Viewpoint is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant was picked to keep the day varied rather than repeat an earlier stop.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=3, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,viewpoint,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=3
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=3
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Dense capital city — Romantic date

**Machine-verifiable result: PASS**

- Vibe: `romantic` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 5h (17:00–22:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: high (48 relevant POIs, 11 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_capital_0** (bakery) — tier: `core`, 45min, 0.15km from previous
  2. **viewpoint_capital_0** (viewpoint) — tier: `core`, 90min, 0.15km from previous
  3. **restaurant_capital_4** (restaurant) — tier: `core`, 90min, 0.73km from previous
  4. **Old Quarter Independent Theatre** (theatre) — tier: `core`, 105min, 0.31km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Viewpoint is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant fits the romantic pace you asked for.
  4. This Theatre is a distinctive local find, not just the nearest convenient option.

**Whole-day summary:** Today's plan spans a fuller day with varied stops nearby.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=71
  - [PASS] hard constraint (excluded category) never appears — bakery,viewpoint,restaurant,theatre
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] no duplicate categories in a dense area — duplicateCategoryCount=0
  - [PASS] compact route (avg leg < 0.8km) — avgLegKm=0.43
  - [PASS] sufficient stop variety (no category repeats) — distinctCategories=4/4

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Suburban area — Romantic date

**Machine-verifiable result: PASS**

- Vibe: `romantic` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 5h (17:00–22:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: medium (19 relevant POIs, 8 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_suburb_0** (bakery) — tier: `core`, 45min, 0.40km from previous
  2. **viewpoint_suburb_0** (viewpoint) — tier: `core`, 90min, 0.28km from previous
  3. **restaurant_suburb_1** (restaurant) — tier: `core`, 90min, 1.23km from previous
  4. **bar_suburb_1** (bar) — tier: `core`, 105min, 0.63km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Viewpoint is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant fits the romantic pace you asked for.
  4. This Bar fits the romantic pace you asked for.

**Whole-day summary:** Today's plan is paced to match what you asked for.
**Trade-off note:** Nearby options were limited, so one stop is a little farther away than usual.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=23
  - [PASS] hard constraint (excluded category) never appears — bakery,viewpoint,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] not forced into an urban (high-density) shape given medium supply — densityTier=medium
  - [PASS] coherent transitions (no backtracking) — backtrackCount=0
  - [PASS] mix of local and slightly-farther options — distances=0.40,0.28,1.23,0.63

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Romantic date

**Machine-verifiable result: PASS**

- Vibe: `romantic` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 5h (17:00–22:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (8 relevant POIs, 6 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_cafe_main** (cafe) — tier: `core`, 63min, 0.20km from previous
  2. **town_park** (park) — tier: `core`, 126min, 0.59km from previous
  3. **town_restaurant_main** (restaurant) — tier: `core`, 126min, 1.60km from previous
  4. **town_bar** (bar) — tier: `core`, 147min, 2.00km from previous

**Per-stop explanations**
  1. This Cafe is close to your previous stop, keeping the route compact.
  2. This Park is close to your previous stop, keeping the route compact.
  3. This Restaurant fits the romantic pace you asked for.
  4. This Bar fits the romantic pace you asked for.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop is a little farther away than usual.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — cafe,park,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop is a little farther away than usual.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Romantic date

**Machine-verifiable result: PASS**

- Vibe: `romantic` · Group: `couple` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 5h (17:00–22:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: flexible · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (4 relevant POIs, 4 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (3)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **The Old Mill Viewpoint — village landmark** (viewpoint) — tier: `core`, 126min, 0.80km from previous
  3. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.64km from previous

**Per-stop explanations**
  1. This Cafe is close to your previous stop, keeping the route compact.
  2. This Viewpoint is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant fits the romantic pace you asked for.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=3, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,viewpoint,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=3
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=3
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Dense capital city — Rainy or cold day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: indoor-lean · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: high (48 relevant POIs, 11 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_capital_0** (bakery) — tier: `core`, 45min, 0.15km from previous
  2. **gallery_capital_0** (gallery) — tier: `core`, 90min, 0.25km from previous
  3. **restaurant_capital_0** (restaurant) — tier: `core`, 90min, 0.29km from previous
  4. **cafe_capital_0** (cafe) — tier: `core`, 105min, 0.08km from previous

**Per-stop explanations**
  1. This Bakery keeps things indoors for today's weather.
  2. This Gallery keeps things indoors for today's weather.
  3. This Restaurant keeps things indoors for today's weather.
  4. This Cafe keeps things indoors for today's weather.

**Whole-day summary:** Today's plan spans a fuller day with varied stops nearby.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=71
  - [PASS] hard constraint (excluded category) never appears — bakery,gallery,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] no duplicate categories in a dense area — duplicateCategoryCount=0
  - [PASS] compact route (avg leg < 0.8km) — avgLegKm=0.27
  - [PASS] sufficient stop variety (no category repeats) — distinctCategories=4/4

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Suburban area — Rainy or cold day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: indoor-lean · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: medium (19 relevant POIs, 8 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_suburb_0** (bakery) — tier: `core`, 45min, 0.40km from previous
  2. **ice_cream_suburb_0** (ice_cream) — tier: `core`, 90min, 0.28km from previous
  3. **restaurant_suburb_0** (restaurant) — tier: `core`, 90min, 0.48km from previous
  4. **cafe_suburb_0** (cafe) — tier: `core`, 105min, 0.28km from previous

**Per-stop explanations**
  1. This Bakery keeps things indoors for today's weather.
  2. This Ice cream keeps things indoors for today's weather.
  3. This Restaurant keeps things indoors for today's weather.
  4. This Cafe keeps things indoors for today's weather.

**Whole-day summary:** Today's plan is paced to match what you asked for.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=23
  - [PASS] hard constraint (excluded category) never appears — bakery,ice_cream,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] not forced into an urban (high-density) shape given medium supply — densityTier=medium
  - [PASS] coherent transitions (no backtracking) — backtrackCount=0
  - [PASS] mix of local and slightly-farther options — distances=0.40,0.28,0.48,0.28

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Rainy or cold day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: indoor-lean · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (7 relevant POIs, 5 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_bakery** (bakery) — tier: `core`, 63min, 0.35km from previous
  2. **town_park** (park) — tier: `lastResort` (relaxed), 126min, 0.84km from previous
  3. **town_restaurant_far** (restaurant) — tier: `core`, 126min, 1.76km from previous
  4. **town_cafe_edge** (cafe) — tier: `core`, 147min, 3.25km from previous

**Per-stop explanations**
  1. This Bakery keeps things indoors for today's weather.
  2. This Park was the closest available option once your usual preferences ran out nearby.
  3. This Restaurant keeps things indoors for today's weather.
  4. This Cafe keeps things indoors for today's weather.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop leans on a fallback pick rather than your top preference.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — bakery,park,restaurant,cafe
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop leans on a fallback pick rather than your top preference.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Rainy or cold day

**Machine-verifiable result: PASS**

- Vibe: `chill` · Group: `couple` · Budget level: `2` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: relaxed · Indoor/outdoor lean: indoor-lean · Budget priority: normal
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (3 relevant POIs, 3 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (2)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.62km from previous

**Per-stop explanations**
  1. This Cafe keeps things indoors for today's weather.
  2. This Restaurant keeps things indoors for today's weather.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=2, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=2
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=2
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Dense capital city — Sparse-location fallback case

**Machine-verifiable result: PASS**

- Vibe: `adventurous` · Group: `solo` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 6h (11:00–17:00)

**Resolved Day Intent Profile**
- Pace: energetic · Indoor/outdoor lean: outdoor-lean · Budget priority: normal
- Hard-excluded categories: ferry_terminal

**Day structure**
- Density tier detected: high (50 relevant POIs, 13 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_capital_0** (bakery) — tier: `core`, 45min, 0.15km from previous
  2. **artwork_capital_0** (artwork) — tier: `core`, 90min, 0.08km from previous
  3. **restaurant_capital_0** (restaurant) — tier: `core`, 90min, 0.27km from previous
  4. **bar_capital_0** (bar) — tier: `core`, 105min, 0.08km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Public art is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Bar is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan spans a fuller day with varied stops nearby.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=71
  - [PASS] hard constraint (excluded category) never appears — bakery,artwork,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] no duplicate categories in a dense area — duplicateCategoryCount=0
  - [PASS] compact route (avg leg < 0.8km) — avgLegKm=0.20
  - [PASS] sufficient stop variety (no category repeats) — distinctCategories=4/4

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Suburban area — Sparse-location fallback case

**Machine-verifiable result: PASS**

- Vibe: `adventurous` · Group: `solo` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 6h (11:00–17:00)

**Resolved Day Intent Profile**
- Pace: energetic · Indoor/outdoor lean: outdoor-lean · Budget priority: normal
- Hard-excluded categories: ferry_terminal

**Day structure**
- Density tier detected: medium (17 relevant POIs, 7 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1×

**Selected stops (4)**
  1. **bakery_suburb_0** (bakery) — tier: `core`, 45min, 0.40km from previous
  2. **shopping_suburb_0** (shopping) — tier: `core`, 90min, 0.53km from previous
  3. **restaurant_suburb_0** (restaurant) — tier: `core`, 90min, 0.22km from previous
  4. **bar_suburb_0** (bar) — tier: `core`, 105min, 0.28km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Shopping is close to your previous stop, keeping the route compact.
  3. This Restaurant is close to your previous stop, keeping the route compact.
  4. This Bar is close to your previous stop, keeping the route compact.

**Whole-day summary:** Today's plan is paced to match what you asked for.
**Trade-off note:** (none — no meaningful relaxation occurred)

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=23
  - [PASS] hard constraint (excluded category) never appears — bakery,shopping,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] not forced into an urban (high-density) shape given medium supply — densityTier=medium
  - [PASS] coherent transitions (no backtracking) — backtrackCount=0
  - [PASS] mix of local and slightly-farther options — distances=0.40,0.53,0.22,0.28

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Sparse-location fallback case

**Machine-verifiable result: PASS**

- Vibe: `adventurous` · Group: `solo` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 6h (11:00–17:00)

**Resolved Day Intent Profile**
- Pace: energetic · Indoor/outdoor lean: outdoor-lean · Budget priority: normal
- Hard-excluded categories: ferry_terminal

**Day structure**
- Density tier detected: low (8 relevant POIs, 6 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_bakery** (bakery) — tier: `core`, 63min, 0.35km from previous
  2. **town_park** (park) — tier: `core`, 126min, 0.84km from previous
  3. **town_restaurant_far** (restaurant) — tier: `core`, 126min, 1.76km from previous
  4. **town_bar** (bar) — tier: `core`, 147min, 1.84km from previous

**Per-stop explanations**
  1. This Bakery is close to your previous stop, keeping the route compact.
  2. This Park matches your adventurous, active pace.
  3. This Restaurant matches your adventurous, active pace.
  4. This Bar matches your adventurous, active pace.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop is a little farther away than usual.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — bakery,park,restaurant,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop is a little farther away than usual.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Sparse-location fallback case

**Machine-verifiable result: PASS**

- Vibe: `adventurous` · Group: `solo` · Budget level: `2` · Weather: `clear` (indoorBias=false)
- Duration requested: 6h (11:00–17:00)

**Resolved Day Intent Profile**
- Pace: energetic · Indoor/outdoor lean: outdoor-lean · Budget priority: normal
- Hard-excluded categories: ferry_terminal

**Day structure**
- Density tier detected: low (4 relevant POIs, 4 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (3)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **The Old Mill Viewpoint — village landmark** (viewpoint) — tier: `core`, 126min, 0.80km from previous
  3. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.64km from previous

**Per-stop explanations**
  1. This Cafe is close to your previous stop, keeping the route compact.
  2. This Viewpoint is a distinctive local find, not just the nearest convenient option.
  3. This Restaurant matches your adventurous, active pace.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** One stop's opening hours couldn't be fully confirmed — worth checking before you go.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=3, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,viewpoint,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=3
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=3
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Town of ~15,000 people — Budget + rainy/cold + low-density (critical case)

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: indoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (8 relevant POIs, 6 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (4)**
  1. **town_bakery** (bakery) — tier: `core`, 63min, 0.35km from previous
  2. **town_park** (park) — tier: `lastResort` (relaxed), 126min, 0.84km from previous
  3. **town_cafe_edge** (cafe) — tier: `core`, 126min, 2.24km from previous
  4. **town_bar** (bar) — tier: `core`, 147min, 1.42km from previous

**Per-stop explanations**
  1. This Bakery keeps things indoors for today's weather.
  2. This Park was the closest available option once your usual preferences ran out nearby.
  3. This Cafe keeps things indoors for today's weather.
  4. This Bar keeps things indoors for today's weather.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop leans on a fallback pick rather than your top preference.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=4, pois available=8
  - [PASS] hard constraint (excluded category) never appears — bakery,park,cafe,bar
  - [PASS] every stop received a non-empty explanation — reasons=4
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] does not fail just because categories are sparse — stops=4
  - [PASS] scarcity reflected honestly in density tier — densityTier=low
  - [PASS] scarcity shapes pacing (stretch) rather than padding stop count — stretchFactor=1.4
  - [PASS] wider travel is explained honestly, not silently — hasFarStop=true, tradeOffNote=Nearby options were limited, so one stop leans on a fallback pick rather than your top preference.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_

---

### Rural village — Budget + rainy/cold + low-density (critical case)

**Machine-verifiable result: PASS**

- Vibe: `budget` · Group: `couple` · Budget level: `1` · Weather: `rainyOrCold` (indoorBias=true)
- Duration requested: 6h (10:00–16:00)

**Resolved Day Intent Profile**
- Pace: moderate · Indoor/outdoor lean: indoor-lean · Budget priority: low
- Hard-excluded categories: nightclub, ferry_terminal

**Day structure**
- Density tier detected: low (4 relevant POIs, 4 distinct categories nearby)
- Slot template used: [coffee, activity, food, evening] · Stretch factor: 1.4×

**Selected stops (3)**
  1. **village_only_cafe** (cafe) — tier: `core`, 63min, 0.15km from previous
  2. **The Old Mill Viewpoint — village landmark** (viewpoint) — tier: `lastResort` (relaxed), 126min, 0.80km from previous
  3. **village_only_restaurant** (restaurant) — tier: `core`, 126min, 0.64km from previous

**Per-stop explanations**
  1. This Cafe keeps things indoors for today's weather.
  2. This Viewpoint was the closest available option once your usual preferences ran out nearby.
  3. This Restaurant keeps things indoors for today's weather.

**Whole-day summary:** Today's plan uses fewer, longer stops since nearby options were limited.
**Trade-off note:** Nearby options were limited, so one stop leans on a fallback pick rather than your top preference.

**Pass/fail against environment-specific criteria**
  - [PASS] never silently empty when valid candidates exist — stops=3, pois available=4
  - [PASS] hard constraint (excluded category) never appears — cafe,viewpoint,restaurant
  - [PASS] every stop received a non-empty explanation — reasons=3
  - [PASS] no unsupported claims in any generated text — 
  - [PASS] small stop count (<=3) treated as a valid result, not padded — stops=3
  - [PASS] no forced 5-slot template — slotTypes=[coffee,activity,food,evening]
  - [PASS] summary does not overstate density — Today's plan uses fewer, longer stops since nearby options were limited.

**Human-review notes (judgment, not asserted by any test):**
- Does the itinerary read as one coherent day, not a list of unrelated stops? _[reviewer to fill in]_
- For village/town: does the day prioritize genuinely distinctive local character, or does it feel like generic filler? _[reviewer to fill in]_
- Does the explanation text read naturally, or does it feel templated/repetitive across stops? _[reviewer to fill in]_
