{
  "name": "dayloop-events-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies Ticketmaster Discovery API requests for DayLoop without exposing the API key to the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
